version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: callcenter-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: callcenter
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: callcenter-api
    restart: always
    environment:
      DATABASE_URL: "mysql://root:root@mysql:3306/callcenter"
      PORT: 3008
    ports:
      - "3008:3008"
    depends_on:
      - mysql
    volumes:
      - ./api:/usr/src/app
      - /usr/src/app/node_modules

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: callcenter-frontend
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules

volumes:
  mysql_data:
